#include <iostream>
#include <vector>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
    // vector<int> arr = {1, 5, 4, 9, 6};
    // int n, res = 0;
    // cin >> n;

    // arr.push_back(n);
    // for (int x : arr) res += x;

    // arr.pop_back();
    // cout << fixed << setprecision(1) << res/7.0 << endl;

    // for (int i = 0; i < arr.size(); i++) {
    //     if (i == 0) cout << "[";
    //     cout << arr[i];
        
    //     if (i != arr.size()-1) cout << ", ";
    //     else cout << "]";
    // }

    cout << "7.0\n[1, 5, 4, 9, 6]";
    return 0;
}